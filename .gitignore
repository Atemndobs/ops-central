# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*.local
.env.production
.env.vercel*
.env.*.production

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# clerk configuration (can include secrets)
/.clerk/

# Runtime-only — scheduled-task lock file
.claude/scheduled_tasks.lock

# Tooling state (per-agent, never commit)
.claude/worktrees/
.claude/settings.json
.codex/
graphify-out/

# Convex AI-files install — multi-tool skill scatter (ignore all)
# Most entries are symlinks to ../.agents/skills/convex; no trailing slash so symlinks match too.
.claude/skills/convex
.commandcode/
.continue/skills/convex
.goose/skills/convex
.kiro/skills/convex
.pi/skills/convex
.qoder/skills/convex
.roo/skills/convex
.trae/skills/convex
.windsurf/skills/convex
skills/convex
SESSIONS.md

# Convex prod snapshots from `csoi backup` — never commit these.
# They contain the full production database (guest names, addresses, financials).
/backups/
